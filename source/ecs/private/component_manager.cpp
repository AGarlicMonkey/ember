#include "ember/ecs/component_manager.hpp"

namespace ember::ecs {
    /* archetype &component_manager::get_archetype(archetype_id_t const &id) {
        for(auto &archetype : archetypes) {
            if(archetype.id == id) {
                return archetype;
            }
        }

        archetypes.emplace_back(id);
    } */
}